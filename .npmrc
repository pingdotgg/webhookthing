# Expo doesn't play nice with pnpm by default. 
# The symbolic links of pnpm break the rules of Expo monorepos.
# @link https://docs.expo.dev/guides/monorepos/#common-issues
node-linker=hoisted
public-hoist-pattern[]=*prisma*

# Theo's grown to believe peer deps were built for a world
# without monorepos. So many problems with Prisma expecting NextAuth,
# trpc expecting Prisma, etc, all are solved by this being false
strict-peer-dependencies=false